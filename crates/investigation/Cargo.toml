[package]
name = "investigation"
version = "0.1.0"
edition = "2024"

[dependencies]
protocol = { path = "../protocol" }
validation = { path = "../validation" }
rusqlite = { version = "0.32", features = ["bundled"] }
serde_json = "1"